# AI Document Platform

The web client for the AI Document Platform. It provides authentication, document upload and preparation, document search, and grounded chat conversations.

## Development

```bash
npm install
npm run dev
```

Set `VITE_API_BASE_URL` in `.env` to the backend API URL before starting the client.

## Checks

```bash
npm run lint
npm run build
```
